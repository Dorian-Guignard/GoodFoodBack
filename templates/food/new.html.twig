{% extends 'base.html.twig' %}

{% block title %}New Food{% endblock %}

{% block body %}
<div class="backshow">
    <h1>Create new Food</h1>

    {{ include('food/_form.html.twig') }}

    <a class="btn btn-info" href="{{ path('app_food_index') }}">back to list</a>
</div>
{% endblock %}

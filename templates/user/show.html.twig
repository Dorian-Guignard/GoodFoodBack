{% extends 'base.html.twig' %}

{% block title %}User{% endblock %}

{% block body %}
<div class = "backshow">
    <h1>Utilisateur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user.id }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ user.email }}</td>
            </tr>
            <tr>
                <th>Roles</th>
                <td>{{ user.roles ? user.roles|json_encode : '' }}</td>
            </tr>
            <tr>
                <th>Mot de passe</th>
                <td>{{ user.password }}</td>
            </tr>
            <tr>
                <th>Pseudo</th>
                <td>{{ user.username }}</td>
            </tr>
            <tr>
                <th>Avatar</th>
                <td>{{ user.nameImage }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-info" href="{{ path('app_user_index') }}">Retour à l'index</a>

    <a class="btn btn-warning " href="{{ path('app_user_edit', {'id': user.id}) }}">Edition</a>

    {{ include('user/_delete_form.html.twig') }}
</div>
{% endblock %}

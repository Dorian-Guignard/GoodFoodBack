{% extends 'base.html.twig' %}

{% block title %}Virtue{% endblock %}

{% block body %}
<div class = "backshow">
    <h1>Vertu</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ virtue.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ virtue.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ virtue.description }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ virtue.nameImage }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-info" href="{{ path('app_virtue_index') }}">Retour à l'index</a>

    <a class="btn btn-warning "  href="{{ path('app_virtue_edit', {'id': virtue.id}) }}">Edition</a>

    {{ include('virtue/_delete_form.html.twig') }}
</div>
{% endblock %}
